{
  "name": "yaml-dts-gen",
  "version": "0.0.3",
  "main": "index.js",
  "repository": "https://github.com/mkusaka/yaml-dts-gen.git",
  "author": "mkusaka <hinoshita1992@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^3.14.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^3.12.5",
    "@types/node": "^14.11.10",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  },
  "bin": {
    "yaml-dts-gen": "bin/gen"
  },
  "scripts": {
    "check": "ts-node -T -O '{\"module\": \"commonjs\"}'",
    "build": "rm -rf dist && tsc",
    "prepare": "yarn build",
    "prepublish": "git push origin HEAD && git push --tags"
  },
  "files": [
    "bin/gen",
    "dist",
    "cli.ts"
  ],
  "keywords": [
    "cli",
    "type definition",
    "generator",
    "yaml",
    "yml"
  ]
}
