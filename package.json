{
  "name": "yaml-dts-gen",
  "version": "0.0.4",
  "main": "index.js",
  "repository": "https://github.com/mkusaka/yaml-dts-gen.git",
  "author": "mkusaka <hinoshita1992@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.2.0",
    "ts-morph": "^15.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^16.18.61",
    "ts-node": "^10.7.0",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "typescript": "*"
  },
  "bin": {
    "yaml-dts-gen": "bin/gen"
  },
  "scripts": {
    "test": "curl https://raw.githubusercontent.com/OAI/OpenAPI-Specification/master/examples/v3.0/petstore.yaml -o petstore.yml && pnpm run build && bin/gen petstore.yml",
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "pnpm run build && git push origin HEAD && git push --tags"
  },
  "files": [
    "bin/gen",
    "dist",
    "cli.ts"
  ],
  "keywords": [
    "cli",
    "type definition",
    "generator",
    "yaml",
    "yml"
  ],
  "packageManager": "pnpm@11.5.2"
}
